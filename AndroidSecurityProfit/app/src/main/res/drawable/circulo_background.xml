<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Círculo de fundo -->
    <item>
        <shape
            android:shape="oval">
            <solid android:color="@color/white" />
            <size android:width="80dp" android:height="80dp" />
        </shape>
    </item>
    <!-- Ícone -->
    <item android:drawable="@drawable/perfil" />
</layer-list>
